<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ind.david.beatpermintue">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <!--
        todo the first activity which will be launched in the app is written here.
        Notice we put the flag "android.intent.action.MAIN" which means that we want this activity
        to run on app start.
        In general, it's good practice to remember that MainActivity should always be the first
        activity on your apps but for the sake of this project it's fine to leave it like so.
        -->
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!--
        todo note we will also write all of the other activities, we will use in the project, here.
        That's why we also written MainActivity here
         -->
        <activity android:name=".MainActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />


        <activity android:name=".page2" />
        <activity android:name="test2.Main2Activity" />
        <activity android:name="test2.Main3Activity" />


    </application>

</manifest>